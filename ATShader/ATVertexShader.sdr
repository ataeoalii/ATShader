attribute vec4 position;

uniform mat4 modelView;
uniform mat4 projection;

void main(void)
{
    vec4 pos = projection * modelView * position;
    vec4 eyePosition = modelView * position;
    gl_Position = vec4(pos.x, pos.y, eyePosition.z * eyePosition.z, pos.w);

}